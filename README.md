To run the following program:
    1.Have python installed
    2.If on Windows run (e.g. double click) the run.bat file
    3.If on Mac run the run.ssh file
    4.If on Linux, you don't get to run it :(